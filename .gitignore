# Build inputs and dependencies — reproducible from tools/sources.json and
# tools/package-lock.json, so never committed.
tools/node_modules/
tools/.cache/

# Screenshots and scratch output from assistant sessions — not project files.
Claude outputs/
